import 'package:flutter/material.dart';

class FocusRepartitionSerie {
  final String type;
  final int count;
  final Color color;

  const FocusRepartitionSerie(
      {required this.type, required this.count, this.color = Colors.orange});
}
