import 'package:flutter/material.dart';

class MuscleActivationRepartitionGraph extends StatelessWidget {
  const MuscleActivationRepartitionGraph({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
