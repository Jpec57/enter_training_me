import 'package:flutter/material.dart';

class TrainingHistoryEvolution extends StatelessWidget {
  const TrainingHistoryEvolution({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}