import 'package:flutter/material.dart';

class WorkoutShowPage extends StatelessWidget {
  static const routeName = "/workout/show";

  const WorkoutShowPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
