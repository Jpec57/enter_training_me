import 'package:flutter/material.dart';

class UserRankingCard extends StatelessWidget {
  final int rank;
  const UserRankingCard({Key? key, required this.rank}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
