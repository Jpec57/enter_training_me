import 'package:flutter/material.dart';

class ChronologicalArrow extends StatelessWidget {
  const ChronologicalArrow({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}